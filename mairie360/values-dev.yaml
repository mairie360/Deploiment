# -----------------------------
# Valeurs globales pour l'environnement dev
# -----------------------------
global:
  namespace: dev
  imagePullPolicy: IfNotPresent
  resources:
    requests:
      memory: "256Mi"
      cpu: "250m"

# -----------------------------
# Sous-charts activés et configurés pour dev
# -----------------------------
database:
  enabled: true
  image:
    repository: "ghcr.io/mairie360/database"
    tag: "main-18242192009"
  env:
    POSTGRES_USER: "postgres"
    POSTGRES_PASSWORD: "dev_pass"
    POSTGRES_DB: "core_dev"
  persistence:
    enabled: true
    size: "1Gi"

  redis:
    enabled: true
    replicaCount: 1
    image:
      tag: "7.2.0"          # tag que tu veux utiliser pour dev
    redis:
      password: "devpassword" # mot de passe Redis pour dev
    persistence:
      enabled: false
    config:
      maxmemory: "256mb"
      maxmemory-policy: "allkeys-lru"

# APIs:
#   enabled: true
#   image:
#     tag: "main"
#   replicaCount: 1

# BFFs:
#   enabled: true
#   image:
#     tag: "main"
#   replicaCount: 1

# Fronts:
#   enabled: true
#   image:
#     tag: "main"
#   replicaCount: 1
