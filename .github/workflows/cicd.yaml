name: Helm CI/CD

on:
  push:
    branches: [ main, develop ]
  pull_request:

jobs:
  # ================================
  # 🧱 JOB 1 : Validation & Qualité
  # ================================
  helm-validation:
    name: Lint & Template Validation
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.15.0

      - name: Helm dependency build
        run: helm dependency build ./charts/mairie360-stack

      - name: Helm lint
        # Les subcharts (APIs/BFFs/Fronts) ne sont pas autonomes : ils dépendent
        # des valeurs `global.*` fournies par l'umbrella. On lint donc l'umbrella,
        # le rendu par env ci-dessous valide les subcharts avec de vraies valeurs.
        run: helm lint ./charts/mairie360-stack

      # Render every instance and validate it against the Kubernetes schemas.
      # CiliumNetworkPolicy has no schema in kubeconform's default store.
      - name: Install kubeconform
        run: |
          curl -sSL https://github.com/yannh/kubeconform/releases/download/v0.6.7/kubeconform-linux-amd64.tar.gz \
            | sudo tar -xz -C /usr/local/bin kubeconform

      - name: Render and validate every instance
        run: |
          for v in clusters/*/instances/*; do
            echo "Rendering $v"
            helm template r ./charts/mairie360-stack -f "$v/values.yaml" \
              | kubeconform -strict -summary -schema-location default -skip CiliumNetworkPolicy
          done

      - name: Helm unit tests
        run: |
          helm plugin install https://github.com/helm-unittest/helm-unittest --version v0.8.2
          helm unittest ./charts/mairie360-stack

  # # ================================
  # # ⚙️ JOB 2 : Tests Fonctionnels (Matrix)
  # # ================================
  # helm-tests:
  #   name: Functional Tests
  #   needs: helm-validation
  #   runs-on: ubuntu-latest
  #   strategy:
  #     matrix:
  #       component: [database, redis, full] # On parallélise pour gagner du temps
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@v4

  #     - name: Start Minikube
  #       uses: medyagh/setup-minikube@latest
  #       with:
  #         memory: 6g
  #         cpus: 2

  #     - name: Deploy & Test ${{ matrix.component }}
  #       run: |
  #         # Logique simplifiée : on déploie selon le composant de la matrix
  #         # Utilisation des fichiers de values réels de ton repo
  #         helm upgrade --install mairie360-${{ matrix.component }} ./charts/mairie360-stack \
  #           -f ./clusters/local/dev/values.yaml \
  #           --set ${{ matrix.component }}.enabled=true \
  #           -n test --create-namespace
          
  #         kubectl wait --for=condition=Ready pods --all -n test --timeout=300s
  #         helm test mairie360-${{ matrix.component }} -n test