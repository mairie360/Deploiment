# Client pods labelled as an API (the only component allowed to reach both
# Postgres and Redis), each reading core-api's credentials from the Secrets
# the chart wires into the real core-api Deployment.
apiVersion: v1
kind: Pod
metadata:
  name: client-redis
  labels: { app.kubernetes.io/component: api }
spec:
  terminationGracePeriodSeconds: 0
  containers:
    - name: redis
      image: redis:7.4-alpine
      command: ["sleep", "3600"]
      env:
        - name: REDIS_HOST
          value: (join('-', [$release, 'redis']))
        - name: ROLE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: (join('-', [$release, 'redis']))
              key: core-api-password
        - name: ADMIN_PASSWORD
          valueFrom:
            secretKeyRef:
              name: (join('-', [$release, 'redis']))
              key: redis-password
        # MAIR-264: the revocation list checks use a reader API and a BFF.
        - name: PROJECT_API_PASSWORD
          valueFrom:
            secretKeyRef:
              name: (join('-', [$release, 'redis']))
              key: project-api-password
        - name: USER_BFF_PASSWORD
          valueFrom:
            secretKeyRef:
              name: (join('-', [$release, 'redis']))
              key: user-bff-password
        # Same construction as the APIs chart: REDIS_URL carries the role's
        # credentials through Kubernetes $(VAR) expansion.
        - name: REDIS_USERNAME
          value: project-api
        - name: REDIS_PASSWORD
          valueFrom:
            secretKeyRef:
              name: (join('-', [$release, 'redis']))
              key: project-api-password
        - name: REDIS_URL
          value: (join('', ['redis://$(REDIS_USERNAME):$(REDIS_PASSWORD)@', $release, '-redis:6379']))
---
apiVersion: v1
kind: Pod
metadata:
  name: client-postgres
  labels: { app.kubernetes.io/component: api }
spec:
  terminationGracePeriodSeconds: 0
  containers:
    - name: postgres
      image: postgres:17-alpine
      command: ["sleep", "3600"]
      env:
        - name: PGHOST
          value: (join('-', [$release, 'database']))
        - name: PGDATABASE
          valueFrom:
            secretKeyRef:
              name: (join('-', [$release, 'database-secret']))
              key: POSTGRES_DB
        - name: PGPASSWORD
          valueFrom:
            secretKeyRef:
              name: (join('-', [$release, 'database-secret']))
              key: CORE_API_PASSWORD
