# 1. Image & Global
image:
  pullSecretData: "eyJhdXRocyI6eyJnaGNyLmlvIjp7ImF1dGgiOiJVWFZsYm5ScGJuUnVjbXc2WjJod1gwOUVjVUV5YzJ0dlIwY3diR001UjBSd1VHOUtUV1pGY1VWUVUwZ3pNak4zVHpkS1FnPT0ifX19"
  repository: ghcr.io/mairie360/core
  tag: "latest"

# 2. Global
global:
  domain: "dev.mairie360-eip.fr"
  # Configuration des BFFs (ce que vous avez probablement déjà fait)

  database:
    type: "postgres"
    host: "mairie360-database" # Nom du service Kubernetes
    port: 5432
    name: "mairie_db_dev"
    user: "postgres" # Si vous utilisez des secrets, voir note ci-dessous
    secretName: "local-dev-database-secret"

  redis:
    host: "mairie360-redis"
    port: 6379
    url: "redis://mairie360-redis:6379"

  bffs:
    instances:
      user-bff: { port: 4000 }
      project-bff: { port: 4001 }
      calendar-bff: { port: 4002 }
      message-bff: { port: 4003 }
      email-bff: { port: 4004 }
      files-bff: { port: 4005 }
      elearning-bff: { port: 4006 }

  # Ajoutez ce bloc pour les APIs :
  apis:
    instances:
      core-api: { port: 3000 }
      project-api: { port: 3001 }
      calendar-api: { port: 3002 }
      message-api: { port: 3003 }
      email-api: { port: 3004 }
      files-api: { port: 3005 }
      elearning-api: { port: 3006 }

# 3. Ingress
ingress:
  enabled: true
  className: "nginx"
  clusterIssuer: "letsencrypt-local"
  annotations:
    # --- FIX POUR LET'S ENCRYPT ---
    # Autorise le HTTP sur le port 80 pour que le challenge ACME passe
    kubernetes.io/ingress.allow-http: "true"
    # Désactive la redirection SSL forcée par Nginx le temps de la validation
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "false"
    # Sécurité et compatibilité
    nginx.ingress.kubernetes.io/whitelist-source-range: "0.0.0.0/0"
  hosts:
    - host: dev.mairie360-eip.fr
      paths:
        - path: /
          pathType: ImplementationSpecific
          serviceName: "local-dev-fronts"
          servicePort: 80
  tls:
    - secretName: dev-mairie360-tls
      hosts:
        - dev.mairie360-eip.fr

# 4. Service
service:
  type: ClusterIP
  port: 80

# 5. Database
database:
  resources:
    requests:
      memory: "128Mi"
      cpu: "100m"
  persistence:
    size: "1Gi"
    storageClass: "local-path"
  env:
    POSTGRES_USER: "postgres"
    POSTGRES_PASSWORD: "mysecretpassword"
    POSTGRES_DB: "mairie_db_dev"

# 6. Liquibase
liquibase:
  image:
    tag: "dev-latest"
  db:
    host: "local-dev-database"
    port: 5432
    name: "mairie_db_dev"
  secretName: "local-dev-database-secret"

# 7. Redis
redis:
  enabled: true

# 8. APIs
APIs:
  instances:
    core-api:
      enabled: true
      image:
        tag: "dev-4048375"
        repository: ghcr.io/mairie360/core-api

    project-api:
      enabled: true
      image:
        tag: "dev-89c9730"
        repository: ghcr.io/mairie360/project-api

    calendar-api:
      enabled: true
      image:
        tag: "dev-d6d05a0"
        repository: ghcr.io/mairie360/calendar-api

    message-api:
      enabled: true
      image:
        tag: "dev-37b72aa"
        repository: ghcr.io/mairie360/message-api

    email-api:
      enabled: true
      image:
        tag: "dev-latest"
        repository: ghcr.io/mairie360/email-api

    files-api:
      enabled: true
      image:
        tag: "dev-latest"
        repository: ghcr.io/mairie360/files-api

    elearning-api:
      enabled: true
      image:
        tag: "dev-1054189"
        repository: ghcr.io/mairie360/elearning-api

# 9. BFFs
BFFs:
  instances:
    user-bff:
      enabled: true
      port: 4000
      image:
        tag: "dev-4b75c8e"
        repository: ghcr.io/mairie360/bff-user
      env:
        - name: COOKIE_DOMAIN
          value: ".dev.mairie360-eip.fr"
        - name: JWT_SECRET
          value: 'b"secret"'

    project-bff:
      enabled: true
      port: 4001
      image:
        tag: "dev-a63e9b2"
        repository: ghcr.io/mairie360/bff-project
      env:
        - name: USER_BFF_URL
          value: "http://local-dev-user-bff:4000"

    calendar-bff:
      enabled: true
      port: 4002
      image:
        tag: "dev-f100deb"
        repository: ghcr.io/mairie360/bff-calendar
      env:
        - name: CALENDAR_API_BASE_PATH
          value: "http://local-dev-calendar-api:3002/api"

    message-bff:
      enabled: true
      port: 4003
      image:
        tag: "dev-bebfa8d"
        repository: ghcr.io/mairie360/bff-message
      env:
        - name: MESSAGE_API_BASE_PATH
          value: "http://local-dev-message-api:3003/api"

    email-bff:
      enabled: true
      port: 4004
      image:
        tag: "dev-latest"
        repository: ghcr.io/mairie360/bff-email
        env:
          - name: PORT
            value: "4004"

    files-bff:
      enabled: true
      port: 4005
      image:
        tag: "dev-latest"
        repository: ghcr.io/mairie360/bff-files
        env:
          - name: PORT
            value: "4005"

    elearning-bff:
      enabled: true
      port: 4006
      image:
        tag: "dev-7ba7c93"
        repository: ghcr.io/mairie360/bff-elearning
      env:
        - name: USER_BFF_URL
          value: "http://local-dev-user-bff:4000"

# 10. Fronts
Fronts:
  instances:
    login-front:
      enabled: true
      image:
        tag: "dev-08032c2"
        repository: ghcr.io/mairie360/login-front
      env:
        - name: COOKIE_DOMAIN
          value: ".dev.mairie360-eip.fr"

    project-front:
      enabled: true
      image:
        tag: "dev-0a88185"
        repository: ghcr.io/mairie360/projects-front
      env:
        - name: COOKIE_DOMAIN
          value: ".dev.mairie360-eip.fr"

    calendar-front:
      enabled: true
      image:
        tag: "dev-b3bc020"
        repository: ghcr.io/mairie360/calendar-front
      env:
        - name: COOKIE_DOMAIN
          value: ".dev.mairie360-eip.fr"

    message-front:
      enabled: true
      image:
        tag: "dev-39a9e87"
        repository: ghcr.io/mairie360/message-front
      env:
        - name: COOKIE_DOMAIN
          value: ".dev.mairie360-eip.fr"

    email-front:
      enabled: true
      image:
        tag: "dev-latest"
        repository: ghcr.io/mairie360/email-front

    files-front:
      enabled: true
      image:
        tag: "dev-latest"
        repository: ghcr.io/mairie360/files-front

    elearning-front:
      enabled: true
      image:
        tag: "dev-7b2585e"
        repository: ghcr.io/mairie360/elearning-front
      env:
        - name: COOKIE_DOMAIN
          value: ".dev.mairie360-eip.fr"

    admin-front:
      enabled: true
      image:
        tag: "dev-1e0bcf0"
        repository: ghcr.io/mairie360/administrator-front
      env:
        - name: COOKIE_DOMAIN
          value: ".dev.mairie360-eip.fr"
