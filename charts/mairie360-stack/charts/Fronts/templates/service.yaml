apiVersion: v1
kind: Service
metadata:
  name: {{ include "fronts.fullname" . }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000 # On cible le port 3000 du pod
      protocol: TCP
      name: http
  selector:
    {{- include "fronts.selectorLabels" . | nindent 4 }}